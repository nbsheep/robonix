[contract]
id      = "robonix/primitive/drone/camera_capture"
version = "1"
kind    = "primitive"
idl     = "drone/srv/CameraCapture.srv"
description = "触发相机拍照（单张）：调用 /api/camera {action:\"photo\"}，仅巡航中可用（cruiseActive==true）。"

[mode]
type = "rpc"
