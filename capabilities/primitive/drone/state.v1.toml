[contract]
id      = "robonix/primitive/drone/state"
version = "1"
kind    = "primitive"
idl     = "drone/srv/State.srv"
description = "获取无人机完整状态（任务状态/操作模式/高度/GPS/巡航状态/SDK状态）。GPS 经 /api/capture_gps 单独获取。"

[mode]
type = "rpc"
