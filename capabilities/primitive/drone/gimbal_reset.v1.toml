[contract]
id      = "robonix/primitive/drone/gimbal_reset"
version = "1"
kind    = "primitive"
idl     = "drone/srv/GimbalReset.srv"
description = "云台回中：恢复到初始中心姿态（默认 pitch=0/roll=0/yaw=0，即机头正前方水平），可选自定义回中目标角。内部经 /api/gimbal 绝对角度下发，并同步更新客户端跟踪的云台姿态（避免后续增量旋转基准漂移）。"

[mode]
type = "rpc"
