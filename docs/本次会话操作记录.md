# 本次会话操作记录

> 时间：2026-08-12
> 会话目标：配置 drone_bridge 连接 RC Pro、验证原语可用性

---

## 一、用户需求

用户的 RC Pro 和电脑连到了同一网络（IP: `10.225.57.15`），需要：
1. 将所有配置文件中的默认 IP 改为实际 IP
2. 在 Robonix 中验证 drone_bridge 原语正确注册并可用

---

## 二、已修改的文件

共修改 **5 个文件**，7 处 IP 地址，全部从 `192.168.1.100` → `10.225.57.15`：

| 文件 | 修改内容 |
|------|----------|
| `drone_bridge/main.py` | 默认 `RC_PRO_IP` |
| `drone_bridge/driver.py` | 2 处默认 `RC_PRO_IP` |
| `scripts/start.sh` | 2 处 fallback IP |
| `package_manifest.yaml` | config 默认值 |
| `robonix_manifest.yaml` | 部署清单 IP |

---

## 三、已验证的结果

### 3.1 网络连通性 ✅

```bash
curl http://10.225.57.15:8080/api/status
# → {"sdkRegistered":true, "productConnected":true, "missionState":"IDLE", ...}
```

### 3.2 Standalone 模式 ✅

Windows 侧 Python 直接调用 `DroneClient` + `CommandHandler`：
- `state_battery` → `{"percent": 0.0}`（结构正确，APK 暂未暴露电量字段）
- `state_position` → `{"latitude": null, "longitude": null, ...}`（室内无 GPS 信号，正常）

### 3.3 rbnx boot ✅

```
    ____        __                 _
   / __ \____  / /_  ____  ____  (_)  __
  / /_/ / __ \/ __ \/ __ \/ __ \/ / |/_/
 / _, _/ /_/ / /_/ / /_/ / / / / />  <
/_/ |_|\____/_.___/\____/_/ /_/_/_/|_|
        Embodied AI Operating System

✓ 4 component(s) up
```

### 3.4 drone_bridge 注册状态 ✅

```
● drone_bridge [ACTIVE] robonix/primitive/drone (8 caps)
```

日志确认：
```
[drone_bridge] RC Pro: 10.225.57.15:8080
[drone_bridge] ✅ 已连接到 RC Pro (http://10.225.57.15:8080)
registered primitive 'drone_bridge'
wired @provides_grpc robonix/primitive/drone/takeoff → ...
wired @provides_grpc robonix/primitive/drone/land → ...
... (全部 8 个能力)
state REGISTERED → INACTIVE → ACTIVE
```

### 3.5 能力列表 ✅

```
● drone_bridge [ACTIVE]
    └─ robonix/primitive/drone/driver (grpc)
    └─ robonix/primitive/drone/takeoff (grpc)
    └─ robonix/primitive/drone/land (grpc)
    └─ robonix/primitive/drone/move_ee (grpc)
    └─ robonix/primitive/drone/hover (grpc)
    └─ robonix/primitive/drone/rth (grpc)
    └─ robonix/primitive/drone/state_position (grpc)
    └─ robonix/primitive/drone/state_battery (grpc)
```

---

## 四、新增文件

| 文件 | 说明 |
|------|------|
| `docs/原语验证指南.md` | 给用户的自助验证操作文档 |
| `docs/本次会话操作记录.md` | 本文件 |
| `test_verify.py` | 自动化验证脚本 |

---

## 五、当前部署拓扑

```
M3E 无人机
    │ OcuSync (大疆私有链路)
    ▼
RC Pro (Android, IP: 10.225.57.15)
    │ Drone_test APK → WebServer :8080
    │ WiFi
    ▼
PC (Windows 11)
    ├─ 可直接访问 http://10.225.57.15:8080
    │
    └─ WSL2 (Ubuntu 22.04)
        └─ ~/my-robot/
            ├─ robonix_manifest.yaml  ← 含 drone_bridge 配置
            ├─ primitives/drone_bridge/  ← 原语代码（已同步最新）
            └─ rbnx-boot/logs/drone_bridge.log
```

---

## 六、常用命令速查

```bash
# ── Windows 侧 ──
curl http://10.225.57.15:8080/api/status               # 测连通
cd C:\Users\nice\Desktop\drone_bridge && python drone_bridge\main.py  # REPL

# ── WSL2 侧 ──
export PATH="$HOME/.cargo/bin:$PATH"
cd ~/my-robot && rbnx boot                              # 启动
rbnx caps | grep drone                                  # 看能力
rbnx describe --provider drone_bridge                   # 看详情
cat ~/my-robot/rbnx-boot/logs/drone_bridge.log          # 看日志
pkill -f robonix                                        # 停所有
python3 /mnt/c/Users/nice/Desktop/drone_bridge/test_verify.py  # 跑验证

# ── 同步代码 ──
rm -rf ~/my-robot/primitives/drone_bridge
cp -r /mnt/c/Users/nice/Desktop/drone_bridge ~/my-robot/primitives/drone_bridge
```
