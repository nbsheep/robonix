# drone_bridge 原语验证指南

> 适用版本：drone_bridge v1.0.0，RC Pro IP: `10.225.57.15`

---

## 前置条件

- [x] RC Pro 开机，连上 WiFi（与 PC 同一网络）
- [x] Drone_test APK 已打开（AutomatedFlightActivity 页面）
- [x] 无人机已开机并与 RC Pro 对频
- [x] PC 与 RC Pro 在同一网络下

---

## 第 1 步：验证网络连通性（Windows / WSL2 均可）

打开终端，执行：

```bash
curl http://10.225.57.15:8080/api/status
```

**期望输出：** JSON 格式的飞行状态，包含 `"sdkRegistered":true` 和 `"productConnected":true`

如果失败：
- 检查 RC Pro WiFi 是否连接
- 检查 Drone_test APK 是否打开
- 检查 IP 是否正确（RC Pro 设置 → WLAN → 查看 IP）

---

## 第 2 步：Standalone 模式测试（Windows，不需要 Robonix）

```powershell
cd C:\Users\nice\Desktop\drone_bridge
python drone_bridge\main.py
```

> 注意：Windows 上用 `python` 不是 `python3`

进入交互 REPL 后，安全地测试查询类命令：

```
drone> status        # 查看完整飞行状态
drone> bat           # 查电量
drone> pos           # 查当前位置
drone> q             # 退出
```

**期望输出：**
```
drone> bat
{"percent": 0.0}

drone> pos
{"latitude": null, "longitude": null, "altitude": 0.0, "heading": 0.0}
```

⚠️ **不要在这里测 `takeoff` / `land` / `move_ee`**，除非飞机在室外安全环境下！

---

## 第 3 步：rbnx boot 集成测试（WSL2）

### 3.1 启动 WSL2

```powershell
wsl
```

### 3.2 确认文件是最新的

```bash
grep "10.225.57.15" ~/my-robot/primitives/drone_bridge/drone_bridge/main.py
```

应该输出：`RC_PRO_IP = os.environ.get("RC_PRO_IP", "10.225.57.15")`

如果还是旧 IP，重新同步：

```bash
rm -rf ~/my-robot/primitives/drone_bridge
cp -r /mnt/c/Users/nice/Desktop/drone_bridge ~/my-robot/primitives/drone_bridge
```

### 3.3 确认 manifest 配置正确

```bash
cat ~/my-robot/robonix_manifest.yaml
```

应包含：
```yaml
primitive:
  - name: drone_bridge
    path: ./primitives/drone_bridge
    config:
      rc_pro_ip: "10.225.57.15"
      rc_pro_port: 8080
```

### 3.4 启动 rbnx boot

```bash
export PATH="$HOME/.cargo/bin:$PATH"
cd ~/my-robot
rbnx boot
```

**期望输出：**
```
✓ 4 component(s) up; logs under /home/nice/my-robot/rbnx-boot/logs
```

### 3.5 检查 drone_bridge 日志

```bash
cat ~/my-robot/rbnx-boot/logs/drone_bridge.log
```

**关键行：**
```
[drone_bridge] RC Pro: 10.225.57.15:8080
[drone_bridge] ✅ 已连接到 RC Pro (http://10.225.57.15:8080)
registered primitive 'drone_bridge'
state REGISTERED -> INACTIVE
state INACTIVE -> ACTIVE
```

### 3.6 查看已注册的能力

```bash
rbnx caps | grep drone
```

**期望输出：**
```
● drone_bridge [ACTIVE] robonix/primitive/drone (8 caps)
```

### 3.7 查看能力详情

```bash
rbnx describe --provider drone_bridge
```

**期望输出：**
```
● drone_bridge [ACTIVE]
    └─ robonix/primitive/drone/driver (grpc)
    └─ robonix/primitive/drone/takeoff (grpc)
    └─ robonix/primitive/drone/land (grpc)
    └─ robonix/primitive/drone/move_ee (grpc)
    └─ robonix/primitive/drone/hover (grpc)
    └─ robonix/primitive/drone/rth (grpc)
    └─ robonix/primitive/drone/state_position (grpc)
    └─ robonix/primitive/drone/state_battery (grpc)
```

---

## 第 4 步：运行自动化验证脚本

```bash
cd ~/my-robot/primitives/drone_bridge
python3 /mnt/c/Users/nice/Desktop/drone_bridge/test_verify.py
```

**期望输出：**
```
============================================================
  drone_bridge 原语验证
============================================================

[1/3] RC Pro HTTP API 连通性
  ✅ HTTP 200 — SDK已激活: True, 飞机已连接: True, 状态: IDLE

[2/3] 原语映射 (CommandHandler)
  ✅ state_battery: {'percent': 0.0}
  ✅ state_position: {'latitude': None, ...}

[3/3] Robonix 集成状态
  rbnx caps 显示: ● drone_bridge [ACTIVE] (8 caps)

============================================================
  验证完成 — drone_bridge 原语可用！
============================================================
```

---

## 快速排查

| 症状 | 检查项 |
|------|--------|
| `curl` 不通 | RC Pro WiFi 是否连接？IP 是否变了？APK 是否打开？|
| `curl` 通但 SDK 未激活 | APK 页面是否显示 "✓ SDK已激活"？ |
| `rbnx` 命令找不到 | `export PATH="$HOME/.cargo/bin:$PATH"` |
| `rbnx boot` 报端口占用 | `pkill -f robonix` 清理旧进程 |
| drone_bridge 日志显示 RC Pro 不可达 | 检查 WSL2 网络 — 从 Windows 侧 `curl` 先确认能通 |
| `rbnx caps` 没有 drone_bridge | 检查 manifest 是否配置了 drone_bridge primitive |

---

## 8 个原语能力一览

| 能力 ID | 类型 | 说明 | 安全调用（飞机不开） |
|---------|------|------|---------------------|
| `drone/driver` | gRPC | 驱动管理 | - |
| `drone/takeoff` | gRPC | 起飞悬停 | ❌ 需飞机开机 |
| `drone/land` | gRPC | 原地降落 | ❌ 需飞机开机 |
| `drone/move_ee` | gRPC | 飞至目标 GPS | ❌ 需飞机开机 |
| `drone/hover` | gRPC | 紧急悬停 | ❌ 需飞机开机 |
| `drone/rth` | gRPC | 智能返航 | ❌ 需飞机开机 |
| `drone/state_position` | gRPC | 查询位置 | ✅ 可随时调用 |
| `drone/state_battery` | gRPC | 查询电量 | ✅ 可随时调用 |
